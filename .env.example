# Optional. If set, /projects/new uses Claude (Sonnet 4.6) to draft sector,
# duration and team composition from the briefing. Without it, a deterministic
# heuristic is used instead.
ANTHROPIC_API_KEY=

# Override storage paths (optional)
# IMPACTA_DB_PATH=
# IMPACTA_UPLOADS_DIR=
